services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: recos
      POSTGRES_USER: recos
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
